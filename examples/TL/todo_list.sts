// This class abstracts the concept of a TodoList, which has a name and is composed of Tasks.
class TodoList {
    attribute name: string;
    composed_of tasks: Task[1..*];
}

// This class abstracts the concept of a Task, which has a title, description, status, and a list of subtasks.
// It uses the Builder interface to indicate that the generated code will include a Builder class for enabling step-by-step construction of Task objects.
class Task implements Builder {
    attribute title: string;
    attribute description: string;
    attribute status: string;
    attribute completed: boolean;
    composed_of subtasks: Task[0..*];
}
